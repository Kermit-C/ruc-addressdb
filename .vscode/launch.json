{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Address DB",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/dist/addressdb",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Compile Address DB"
        }
    ]
}